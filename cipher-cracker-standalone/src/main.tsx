import React from 'react';
import ReactDOM from 'react-dom/client';
import { CipherCrackerGame } from './CipherCrackerGame';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CipherCrackerGame />
  </React.StrictMode>
);
