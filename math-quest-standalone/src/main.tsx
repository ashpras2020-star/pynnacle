import React from 'react';
import ReactDOM from 'react-dom/client';
import { MathQuestGame } from './MathQuestGame';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MathQuestGame />
  </React.StrictMode>
);
