import React from 'react';
import ReactDOM from 'react-dom/client';
import { BooleanBouncerGame } from './BooleanBouncerGame';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BooleanBouncerGame />
  </React.StrictMode>
);
