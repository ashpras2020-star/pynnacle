import React from 'react';
import ReactDOM from 'react-dom/client';
import { RobotCommanderGame } from './RobotCommanderGame';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RobotCommanderGame />
  </React.StrictMode>
);
